@import '../node_modules/stylus-font-face/lib/font-face.styl';
// @import 'font-face'

html {
   font-size: 1em
   line-height: 1.4
   // font-family: Lulu Mono
   // font-family: Fivo Sans Modern
   // font-family: Space Mono;
   font-family: Space Mono;
   color: white; 
   background-color: black;
   margin: 0;

}
h1{
   // text-shadow: 0 0 10px #FFffff
   text-shadow: 0px 0px 15px #a979ff;
   color: rgba(300,300,300,.9);
}
.centered {
  position: absolute;
  top: 70%;
  left: 50%;
  transform: translate(-50%, -60%);
}
#instagram {
   height: 1.5rem 
}
a {
   text-decoration: none; /* no underline */
   color: white !important
}
#landing h1{
   }

#landing img {
   width: 100%}

//----------GRID SETUP------------//
.grid-container
   display: grid;
nav.grid-container  
   grid-template-columns: repeat(10,1fr)
   grid-template-rows:  repeat(1,1fr)
   align-content: end;

.-8x7 
   grid-template-columns: repeat(8, 1fr)
   grid-template: repeat(7, 1fr)
.-1x3
   grid-template-columns: 1fr
   grid-template-rows: 2rem auto 2rem
.-10x1
   grid-template-columns: repeat(10,1fr)
   grid-template-rows:  repeat(1,1fr)

.nav-item 
   // grid-column: 
   text-align: right;
   a 
#nav-name
   grid-column: 1/span 5

.flex-container
   display: flex;

.flex-item

      //  grid-template-rows: 
// grid-gap: /*column*/ 0 /*row*/ 0;
//   grid-row-gap: 50px;
//   grid-template-columns: 1fr 1fr 1fr; //fracitonal unit and the number of columns and rows in a grid contiainer
//   grid-template-rows: 1fr 1fr 1fr;

//---------REFLECTION EXPERIMENTATION--------//
.text-reflect{  
  -webkit-box-reflect: below 0px -webkit-gradient(linear, left top, left bottom, from(transparent), color-stop(70%, transparent) , to(rgba(250, 250, 250, 0.1)));
//  linear-gradient(transparent, black);
}
.real {
	display:block;
	margin-bottom: 8px;
}
.refl {
	display:block;
	-moz-transform: scaleY(-1);
       -o-transform: scaleY(-1);
       -webkit-transform: scaleY(-1);
       transform: scaleY(-1);
	-webkit-mask-image: -webkit-gradient(linear, right top, right bottom, from(transparent), color-stop(40%, transparent), to(rgba(255, 255, 255, 0.1)));

font-face("Space Mono", "SpaceMono-Regular")
font-face("Lulu Mono", "LuluMonospace-Regular")
/*----------MEDIUM SETUP-----------*/

.fm-post-wrapper { // Surrounding each Medium post
}

.fm-post { // Also surrounding each Medium post
}

.fm-post-image { 
   // The post's leading image
}

.fm-post-title {
    // The title of each post
}

.fm-post-sub-title{ 
   // The intro paragraph
}

.fm-post-read-more {
    // The link to the full post on Medium
}

.fm-link-out { 
   // The link to your Medium profile
}